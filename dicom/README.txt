Basic Python code showing how to iterate over pydicom's dictionary. You can modify it as needed to output more info

Note that Python produces MIME compliant CSV files with Windows newlines - you may need to fix this on other systems.
